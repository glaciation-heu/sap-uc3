PORTNUM = 10000

listen_for_clients(PORTNUM)
socket_id = accept_client_connection(PORTNUM)
v = sint.receive_from_client(2, socket_id)

val1 = v[0]
val2 = v[1]

# The logic
result = val1 < val2 

# Output encrypted result
resp = Array(2, sint)
resp[0] = result
resp[1] = result
sint.reveal_to_clients([socket_id], resp)
closeclientconnection(socket_id)
